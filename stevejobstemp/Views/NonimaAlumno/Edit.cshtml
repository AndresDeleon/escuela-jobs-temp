@model stevejobstemp.Models.NonimaAlumno

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>NonimaAlumno</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="NominaAlumnoId" />
            <div class="form-group">
                <label asp-for="Alumno" class="control-label"></label>
                <select asp-for="AlumnoId" class="form-control" asp-items="@(List<SelectListItem>)ViewBag.alumnoList"></select>
                <span asp-validation-for="AlumnoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Grado" class="control-label"></label>
                <select asp-for="GradoId" class="form-control" asp-items="@(List<SelectListItem>)ViewBag.gradoList"></select>
                <span asp-validation-for="GradoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
