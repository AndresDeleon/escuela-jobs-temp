@model IEnumerable<stevejobstemp.Models.Grado1>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Grado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Grado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GradoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GradoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GradoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
